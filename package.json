{
  "name": "tailwind-project",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "node ./scripts/build-tailwind.js --watch",
    "build": "node ./scripts/build-tailwind.js"
  },
  "postinstall": "echo 'If you experience issues with the Tailwind CLI, ensure Node and npm are up to date and run npm install'",
  "devDependencies": {
    "autoprefixer": "^10.4.21",
    "postcss": "^8.5.6",
    "tailwindcss": "latest"
  },
  "dependencies": {
    "@tailwindcss/postcss": "^4.1.14"
  }
}
